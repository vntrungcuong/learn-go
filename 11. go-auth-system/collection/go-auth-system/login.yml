info:
  name: login
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/api/login
  body:
    type: json
    data: |-
      {
        "email": "cuong@gmail.com",
        "password": "1234567"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
