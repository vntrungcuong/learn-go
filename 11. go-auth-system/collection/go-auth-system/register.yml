info:
  name: register
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/register
  body:
    type: json
    data: |-
      {
        "email": "cuong@gmail.com",
        "password": "1234567",
        "full_name": "Cuong"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
