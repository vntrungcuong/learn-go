info:
  name: reset-password
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8080/api/v1/auth/reset-password
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
