info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/v1/auth/register
  body:
    type: json
    data: |-
      {
        "email": "cuong@gmail.com",
        "password": "123456",
        "fullname": "Cuong"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
