info:
  name: Ping API Server
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/api/v1/ping"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
