info:
  name: Logout
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseURL}}/api/v1/auth/logout"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
