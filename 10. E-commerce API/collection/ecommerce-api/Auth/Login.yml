info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/api/v1/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "test@example.com",
        "password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
